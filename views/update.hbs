<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Movie</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>Update Movie</h1>
        <a href="/">Back to Movie List</a>
    </header>
    
    <form action="/api/movies/update/{{movie.Movie_ID}}" method="POST">
        <!-- Movie ID Field (Hidden) -->
        <input type="hidden" name="Movie_ID" value="{{movie.Movie_ID}}">

<div class="movie-poster">
            {{#if movie.Poster}}
                <img src="{{movie.Poster}}" alt="Movie Poster" style="max-width: 200px; max-height: 300px;">
            {{else}}
                <p>No poster available</p>
            {{/if}}
        </div>
        <!-- Title Field -->
        <input type="text" name="Title" value="{{movie.Title}}" placeholder="Title" required>

        <!-- Release Date Field -->
        <input type="text" name="Released" value="{{movie.Released}}" placeholder="Release Date" required>

        <!-- Submit Button -->
        <button type="submit">Update Movie</button>
    </form>
</body>
</html>
